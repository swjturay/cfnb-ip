# cfnb-ip

Automatically maintained IPv4 and IPv6 Cloudflare endpoint list generated by an external cfnb job.

The current output is stored in [`ip.txt`](ip.txt).

## Format

```text
IPv4: address:port#label
IPv6: [address]:port#label
```
